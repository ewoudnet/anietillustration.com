# Blokkeert alle directe webtoegang tot deze map (PHP-classes, geen public entrypoints).

# Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2 (fallback voor oudere hosting)
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
